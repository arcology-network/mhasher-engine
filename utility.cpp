#include "stdafx.h"

namespace Utility {
	//void SortHash256(char* bytes, uint32_t count, uint32_t* indices) {

	//	char szNumbers[] = "250068492 7b06af00 1100011011110101010001100000 0x6fffff";

	//	std::vector<uint64_t> numbers(count);
	//	for (std::size_t i = 0; i < count; i++) {
	//		char* pEnd;
	//		numbers[i] = strtoull(szNumbers + i * count, &pEnd, 10);
	//	}
	//}
}

